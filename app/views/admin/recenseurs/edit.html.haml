- title = "Modifier un recenseur • Admin"
- content_for(:head_title) { title }

%main#contenu.fr-container.fr-pt-2w.fr-pb-4w
  = render "shared/breadcrumbs", links: [["Admin", admin_path], ["Recenseurs", admin_recenseurs_path]], current_page_label: "Modifier un recenseur"

  %h1
    = icon_span("account-pin-circle", fill: true)
    Modifier #{@recenseur.nom}

  = render "form", recenseur: @recenseur
