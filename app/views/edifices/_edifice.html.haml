%h2.fr-mt-4w{id: edifice.slug}
  = edifice.nom.upcase_first
  ·
  = t("objets.count", count: edifice.objets.length)

.fr-grid-row.fr-pb-4w.fr-grid-row--gutters
  - edifice.objets.each do |objet|
    .fr-col-sm-6.fr-col-md-4.fr-col-lg-3
      - if current_user&.commune == edifice.commune
        = render Communes::ObjetCardComponent.new(objet)
      - elsif current_recenseur && edifice.commune.in?(current_recenseur.communes)
        = render Recenseurs::ObjetCardComponent.new(objet)
      - else
        = render ObjetCardComponent.new(objet)
