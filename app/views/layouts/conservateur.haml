- content_for(:current_layout) { "conservateur" }
- content_for(:below_header) do
  - if current_conservateur != true_conservateur
    .fr-container
      .fr-alert.fr-alert--warning
        .co-flex.co-flex--space-between.co-flex--align-items-center
          %div
            = icon_span "eye"
            Vous incarnez le conservateur #{current_conservateur}
            = icon_span "user-star"
          %div
            = link_to admin_stop_impersonating_conservateur_path, class: "fr-btn fr-btn--secondary fr-btn--icon-right fr-icon-logout-box-r-line", "data-turbo-method": "POST" do
              Arrêter

- content_for(:content) do
  = yield
= render template: "layouts/application"
