<% content_for(:head_title) { "Nouvelle campagne" } %>

<main class="fr-container fr-pt-2w fr-pb-4w">
  <%= render(
    "shared/breadcrumbs",
    links: [
      ["Accueil", root_path],
      ["Campagnes", campaigns_path],
      [@campaign.human_id, campaign_path(@campaign)]
    ],
    current_page_label: "Éditer la configuration"
  ) %>

  <h1>Éditer la campagne <%= @campaign.human_id %></h1>

  <%= render "form", campaign: @campaign %>
</main>
